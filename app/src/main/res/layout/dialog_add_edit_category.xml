<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- Tiêu đề -->
    <TextView
        android:id="@+id/tv_dialog_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:gravity="center"
        android:text="Thêm/Sửa Danh mục"
        android:textColor="@color/brown_700"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Phần nhập Tên danh mục -->
    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:hint="Tên danh mục"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
        app:boxStrokeColor="@color/brown_700"
        app:hintTextColor="@color/brown_700">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/et_category_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textCapWords" />
    </com.google.android.material.textfield.TextInputLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:text="Hình ảnh hiển thị:"
        android:textColor="@color/brown_700"
        android:textStyle="bold"/>

    <!-- Khu vực chọn ảnh -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:layout_marginBottom="12dp">

        <!-- Cột bên trái: Các nút chọn -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">
            
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Icon có sẵn:"
                android:textSize="12sp"
                android:textColor="#757575"/>

            <Spinner
                android:id="@+id/spinner_category_icon"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="48dp" />
                
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="--- HOẶC ---"
                android:layout_gravity="center"
                android:layout_marginVertical="8dp"
                android:textSize="12sp"
                android:textStyle="bold"
                android:textColor="#9E9E9E"/>
                
            <!-- Nút chọn từ thư viện (Đã sửa lỗi namespace) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_select_gallery"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Chọn ảnh khác"
                android:textAllCaps="false"
                android:textSize="13sp"
                app:icon="@drawable/ic_add_photo"
                app:iconGravity="textStart"
                app:backgroundTint="@color/brown_700"/>
        </LinearLayout>

        <!-- Cột bên phải: Ảnh xem trước -->
        <androidx.cardview.widget.CardView
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_marginStart="16dp"
            app:cardCornerRadius="8dp"
            app:cardElevation="4dp">
            
            <ImageView
                android:id="@+id/iv_icon_preview"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/ic_coffee_placeholder"
                android:contentDescription="Ảnh xem trước" />
                
        </androidx.cardview.widget.CardView>
    </LinearLayout>

</LinearLayout>